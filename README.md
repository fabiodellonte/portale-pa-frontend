# portale-pa-frontend

Frontend TypeScript (React + Vite) per Portale PA.

## Principio chiave
Il frontend parla con **API Service** via `VITE_API_BASE_URL`, non direttamente con Supabase REST.

## Avvio
```powershell
cp .env.example .env
npm install
npm run dev
```
